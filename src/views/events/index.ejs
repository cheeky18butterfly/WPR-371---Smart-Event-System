<h1>Events</h1>

<div class="event-grid">

  <% events.forEach(event => { %>

    <div class="event-card">

      <h3><%= event.title %></h3>

      <p><%= event.venue %></p>

      <p><%= event.date %></p>

    </div>

  <% }) %>

</div>